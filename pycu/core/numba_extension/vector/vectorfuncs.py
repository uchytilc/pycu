# from ..mathfuncs import *

def sum(v):
	pass

def dot(v):
	pass

def length(v):
	pass

# def mix(x,y,t)
# 	return x*(1 - t) + y*t


def omit(v, n): #vector, normal (only works on float3)
	#returns a vector of 1 lower dim with the index given removed

	pass
	#return vec2
	#if n == 2
		#return x,y
	#if n == 1
		#return x,z
	#if n == 0
		#return y,z


def shift(v): #moves all values in vector over one
	pass



# def cross(v, w):
# 	pass
# 	# return make_float3(x.y*y.z - x.z*y.y, x.z*y.x - x.x*y.z, x.x*y.y - x.y*y.x);

# def lerp(v, w, t):
# 	pass
# 	# return start + t*(end - start);

# def clamp(v, lo, hi):
# 	pass
# 	# return fmaxf(lo, fminf(f, hi));

# 	# return make_float2(clamp(v.x, lo, hi), clamp(v.y, lo, hi));	

# def smooth_step(start, end, t):
# 	pass
# 	# float2 p = clamp((t - start)/(end - start), 0.f, 1.f);
# 	# return (3.f - 2.f*p)*p*p;

# def param(o, d, t):
# 	pass
# 	# return o + d*t;





# def normalize(vec):
# 	# float invLen = rsqrtf(dot(v, v));
# 	# return v * invLen;
# 	pass

# def frac(vec):
# 	pass

# def mod(veca, vecb):
#     # return make_float2(fmodf(a.x, b.x), fmodf(a.y, b.y));
# 	pass
